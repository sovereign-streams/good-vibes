{
  "name": "good-vibes-provider",
  "version": "0.1.0",
  "type": "module",
  "description": "Good Vibes Stream Provider — SEP-compliant content metadata service",
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
